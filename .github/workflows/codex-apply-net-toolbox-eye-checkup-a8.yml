name: Codex Apply Net Toolbox Eye Checkup A8 Cleanup

on:
  push:
    branches:
      - master
    paths:
      - .github/codex-sync-net-toolbox-eye-checkup-a8.patch.br.b64
      - .github/workflows/codex-apply-net-toolbox-eye-checkup-a8.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Decode and verify patch
        run: |
          node <<'NODE'
          const fs = require('fs');
          const crypto = require('crypto');
          const zlib = require('zlib');
          const payloadPath = '.github/codex-sync-net-toolbox-eye-checkup-a8.patch.br.b64';
          const expected = '56b86696ef44f26e81fb78ac1de1bfcaf8be399d442a9c45a3803dc458cc72f4';
          const compressed = Buffer.from(fs.readFileSync(payloadPath, 'utf8').trim(), 'base64');
          const actual = crypto.createHash('sha256').update(compressed).digest('hex');
          if (actual !== expected) {
            throw new Error(`Patch SHA mismatch: ${actual}`);
          }
          fs.writeFileSync('codex-sync.patch', zlib.brotliDecompressSync(compressed));
          NODE
      - name: Apply patch
        run: |
          git apply --check codex-sync.patch
          git apply codex-sync.patch
      - name: Commit applied source sync
        run: |
          rm -f codex-sync.patch
          rm -f .github/codex-sync-net-toolbox-eye-checkup-a8.patch.br.b64
          rm -f .github/workflows/codex-apply-net-toolbox-eye-checkup-a8.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "sync: replace eye checkup a8 ctas"
          git push origin HEAD:master

name: Codex Apply Net Toolbox Beauty Hair Link Cleanup

on:
  push:
    branches:
      - master
    paths:
      - .github/codex-sync-net-toolbox-beauty-hair-link.patch.br.b64
      - .github/workflows/codex-apply-net-toolbox-beauty-hair-link.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Decode and verify patch
        run: |
          node <<'NODE'
          const fs = require('fs');
          const crypto = require('crypto');
          const zlib = require('zlib');
          const payloadPath = '.github/codex-sync-net-toolbox-beauty-hair-link.patch.br.b64';
          const expected = '3c96d1d326fc6a54ffa70258bb2615e002bc4b02c8d850454bdea4e31208ad83';
          const compressed = Buffer.from(fs.readFileSync(payloadPath, 'utf8').trim(), 'base64');
          const actual = crypto.createHash('sha256').update(compressed).digest('hex');
          if (actual !== expected) {
            throw new Error(`Patch SHA mismatch: ${actual}`);
          }
          fs.writeFileSync('codex-sync.patch', zlib.brotliDecompressSync(compressed));
          NODE
      - name: Apply patch
        run: |
          git apply --check codex-sync.patch
          git apply codex-sync.patch
      - name: Commit applied source sync
        run: |
          rm -f codex-sync.patch
          rm -f .github/codex-sync-net-toolbox-beauty-hair-link.patch.br.b64
          rm -f .github/workflows/codex-apply-net-toolbox-beauty-hair-link.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "sync: avoid retired beauty ranking link"
          git push origin HEAD:master
